---
phase: 01-database-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - "01-00"
files_modified:
  - package.json
  - drizzle.config.ts
  - drizzle/0000_enable_pgvector.sql
autonomous: true
requirements:
  - INFRA-07
  - INFRA-09
tags:
  - drizzle-config
  - pgvector
  - migrations
  - tooling

must_haves:
  truths:
    - "drizzle-kit is installed and `bun db:generate`, `bun db:migrate`, `bun db:push`, `bun db:studio` script aliases exist"
    - "drizzle.config.ts at repo root is valid TypeScript, dialect=postgresql, schema=./src/lib/db/schema.ts, out=./drizzle, schemaFilter=['public']"
    - "drizzle/0000_enable_pgvector.sql exists and contains exactly `CREATE EXTENSION IF NOT EXISTS vector;` (idempotent)"
    - "drizzle.config.ts reads DATABASE_URL via `import { env } from '@/lib/env'` — never raw `process.env`"
    - "package.json `build` script remains UNCHANGED in this plan (Plan 05 owns the migrate→build wiring)"
  artifacts:
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config: postgresql dialect, schema path, out dir, schemaFilter, dbCredentials.url from env.ts"
      contains: "schemaFilter"
    - path: "drizzle/0000_enable_pgvector.sql"
      provides: "Custom migration: enables pgvector extension before any schema migration"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
    - path: "package.json"
      provides: "Adds drizzle-kit + drizzle-orm devDeps and db:* script aliases"
      contains: "db:migrate"
  key_links:
    - from: "drizzle.config.ts"
      to: "src/lib/env.ts"
      via: "import { env } from '@/lib/env' to read DATABASE_URL"
      pattern: "from \"@/lib/env\""
    - from: "drizzle/0000_enable_pgvector.sql"
      to: "drizzle-kit migrate journal"
      via: "tag-ordered migration: 0000 runs before 0001 (Plan 03's schema migration)"
      pattern: "0000_enable_pgvector"
    - from: "package.json scripts.db:*"
      to: "drizzle-kit"
      via: "bun-runs-drizzle-kit invocation"
      pattern: "drizzle-kit"
---

<objective>
Install drizzle-kit + drizzle-orm, create `drizzle.config.ts` (env-driven, schemaFilter=['public']), create the pgvector custom migration `drizzle/0000_enable_pgvector.sql` so the extension is enabled BEFORE any schema migration runs (per RESEARCH §6 ordering rationale; per CONTEXT D-12), and add `db:generate`, `db:migrate`, `db:push`, `db:studio` script aliases following the `<verb>:<noun>` convention.

Purpose: Stand up Drizzle's tooling pipeline in isolation so Plan 03 (schema) can `bun db:generate` immediately, and so the extension is available for any future v0.4 vector column. This plan does NOT modify `package.json` `build` (Plan 05's job — once migrations exist) and does NOT create schema files (Plan 03's job).
Output: drizzle.config.ts, drizzle/0000_enable_pgvector.sql, package.json updated with drizzle-kit + drizzle-orm + db:* scripts.
</objective>

<execution_context>
@/Users/alec/Projects/unsere-news/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alec/Projects/unsere-news/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
@.planning/phases/01-database-foundation/01-VALIDATION.md
@package.json
@biome.json
@tsconfig.json

<interfaces>
From `src/lib/env.ts` (created by Plan 01 — Wave 1 sibling, runs in parallel but depends_on is via files only, not env.ts itself; for SAFETY this plan reads env.ts via path alias and Wave 1 ordering means env.ts must exist when drizzle.config.ts evaluates):

```typescript
export const env: {
  DATABASE_URL: string; // validated via z.string().url()
  // ... other keys not used by drizzle.config.ts
};
```

NOTE on Wave 1 parallelism: this plan and Plan 01 are both Wave 1, both modify package.json (sibling lock-conflict). The orchestrator scheduled them serially within Wave 1. Plan 01 ships `src/lib/env.ts` first; this plan imports from it. If for any reason the executor lands this plan before Plan 01, the import will fail at type-check time — that is the intended hard gate.

From package.json (current — to be augmented):
- `<verb>:<noun>` lowercase script naming
- New scripts must compose with `bun <other-script>` (never `npm run` / `pnpm`)
- `bun = "1.3.11"` per mise.toml is the runtime contract

From RESEARCH §2 pinned versions:
- `drizzle-orm@^0.45.2` (stable, post-fix for Better-Auth × Neon-serverless 1.0 tagged-template bug)
- `drizzle-kit@^0.31.10` (companion to drizzle-orm 0.45.x)

CONTEXT D-13 contract: `drizzle-kit migrate` is the production-grade verification gate. `drizzle-kit push` is dev-only and must NOT be used to verify Phase 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 02-1: Install drizzle-orm + drizzle-kit and add db:* script aliases</name>
  <files>package.json</files>
  <read_first>
    - `package.json` (current scripts and dep ordering — preserve)
    - `01-RESEARCH.md` §2 lines 84-130 (pinned versions: `drizzle-orm@^0.45.2`, `drizzle-kit@^0.31.10`)
    - `01-RESEARCH.md` §1 lines 62-66 (Better-Auth × Neon-serverless bug RESOLVED — pin 0.45.2 with confidence)
    - `01-PATTERNS.md` Convention 5 lines 154-180 (script naming `<verb>:<noun>`, bun-everywhere)
    - `01-PATTERNS.md` §"package.json — modify" lines 768-806 (full diff including db:* scripts)
    - `01-CONTEXT.md` D-13 (drizzle-kit migrate is the production gate; push is dev-only)
  </read_first>
  <action>
    Step A — Install drizzle-orm (dependencies) and drizzle-kit (devDependencies). Pin to RESEARCH §2 ranges:

    ```bash
    bun add drizzle-orm@^0.45.2
    bun add -D drizzle-kit@^0.31.10
    ```

    If `bun add` resolves to a higher major (e.g. drizzle-orm 1.0.x), DO NOT proceed — beta has churned 23+ versions per RESEARCH §14 Pitfall 6. Manually pin in package.json to `^0.45.2` and re-run `bun install`.

    Step B — Augment `package.json` `scripts` section. PRESERVE all existing entries (dev, build, start, check:*, and the test scripts added by Plan 00) in their current ordering. INSERT these four new entries adjacent to the test:* block, grouped together (Biome will sort alphabetically on save if configured; manual placement is fine):

    ```jsonc
    "db:generate": "drizzle-kit generate",
    "db:migrate":  "drizzle-kit migrate",
    "db:push":     "drizzle-kit push",
    "db:studio":   "drizzle-kit studio"
    ```

    DO NOT modify `build` in this plan. Plan 05 prepends `bun db:migrate &&` to `build` — that is the production gate per CONTEXT D-13/D-14. Touching `build` here would break Plan 03 (which generates the schema migration AFTER this plan completes).

    Step C — Verify install: `bunx drizzle-kit --version` (must print 0.31.x). `bun check:format package.json && bun check:ts`.

    Decision-making note: drizzle-kit `generate` and `migrate` are the locked verification path (CONTEXT D-13). `push` is included only because it is useful for local schema iteration during development; it must not be relied on for verification or production. `studio` is a developer convenience (CONTEXT.md "Deferred Ideas" lists it as optional polish but not a Phase 1 success criterion — including it costs nothing and unblocks ad-hoc DB inspection during Plan 03/05 debugging).
  </action>
  <verify>
    <automated>bunx drizzle-kit --version &amp;&amp; grep -F '"db:migrate"' package.json &amp;&amp; grep -F '"db:generate"' package.json &amp;&amp; grep -F '"db:push"' package.json &amp;&amp; grep -F '"db:studio"' package.json &amp;&amp; ! grep -E '"build":\s*"bun db:migrate' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `bunx drizzle-kit --version` prints `0.31.x` (exit 0).
    - `package.json` `dependencies` contains `drizzle-orm` at version satisfying `^0.45.2` (NOT 1.0.x): `grep -E '"drizzle-orm":\s*"\^0\.45\.[0-9]+"' package.json` matches.
    - `package.json` `devDependencies` contains `drizzle-kit` at version satisfying `^0.31.10`.
    - `package.json` `scripts` contains all four entries: `db:generate`, `db:migrate`, `db:push`, `db:studio` — each invoking `drizzle-kit <verb>` (no extra flags).
    - `build` script is UNCHANGED — must still equal `next build` exactly (Plan 05 owns the migrate-prepend): `grep -E '"build":\s*"next build"' package.json` matches.
    - Existing scripts (`dev`, `start`, `check:*`, and `test:*` from Plan 00) are unchanged in name and command.
    - `bun check:format` reports no diffs on package.json.
  </acceptance_criteria>
  <done>
    drizzle-kit and drizzle-orm installed at pinned versions; `bun db:generate`/`db:migrate`/`db:push`/`db:studio` aliases functional. `build` script untouched per Plan 05's contract.
  </done>
</task>

<task type="auto">
  <name>Task 02-2: Create drizzle.config.ts and pgvector custom migration drizzle/0000_enable_pgvector.sql</name>
  <files>drizzle.config.ts, drizzle/0000_enable_pgvector.sql</files>
  <read_first>
    - `01-RESEARCH.md` §5 lines 376-401 (drizzle.config.ts verbatim spec; param notes on schemaFilter)
    - `01-RESEARCH.md` §6 lines 413-461 (migration ordering: 0000 enables extension, 0001 schema; idempotency)
    - `01-RESEARCH.md` §6 lines 432-442 (custom migration generation: `bunx drizzle-kit generate --custom --name=enable_pgvector`)
    - `01-RESEARCH.md` §14 Pitfall 3 lines 1008-1014 (why pgvector enable lives in its own migration)
    - `01-PATTERNS.md` §"drizzle.config.ts" lines 392-419 (verbatim spec + watchpoints)
    - `01-PATTERNS.md` §"drizzle/0000_enable_pgvector.sql" lines 423-440 (generation + content)
    - `01-CONTEXT.md` D-12 (pgvector enabled, NO vector columns in Phase 1)
    - `01-CONTEXT.md` D-14 (Vercel build runs migrations; loud-fail on error — Plan 05 wires this)
  </read_first>
  <action>
    Step A — Create `drizzle.config.ts` at repo root with EXACTLY this content (verbatim from RESEARCH §5):

    ```typescript
    import { defineConfig } from "drizzle-kit";

    import { env } from "@/lib/env";

    export default defineConfig({
      dialect: "postgresql",
      schema: "./src/lib/db/schema.ts",
      out: "./drizzle",
      dbCredentials: {
        url: env.DATABASE_URL,
      },

      // Only manage `public`. The `neon_auth` schema is owned by Neon Auth and
      // must NOT appear in our migrations. Default is ['public'] but we set it
      // explicitly for documentation and to guard against accidental
      // `drizzle-kit pull` introspecting the auth schema.
      // [CITED: orm.drizzle.team/docs/drizzle-config-file]
      schemaFilter: ["public"],

      // Strict mode is OFF to allow build-time `migrate` to run non-interactively
      // on Vercel. Verbose ON for build-log diagnostics.
      strict: false,
      verbose: true,
    });
    ```

    Note (RESEARCH §14 Pitfall 5): `schemaFilter: ['public']` only affects `pull`/`push`, not `generate`/`migrate`. The cross-schema FK to `neon_auth.users_sync` IS emitted from schema.ts (Plan 03) — that is desired behavior, not a regression.

    NOTE: `schema: "./src/lib/db/schema.ts"` points to a file that does NOT yet exist (Plan 03 ships it). This is intentional — drizzle-kit only reads `schema` when `generate`/`migrate`/`push`/`studio` is invoked. The config file itself type-checks because it does not import the schema directly.

    Step B — Create the custom migration directory and pgvector enable migration. drizzle-kit's standard generation flow is:

    ```bash
    bun drizzle-kit generate --custom --name=enable_pgvector
    ```

    This produces an empty `drizzle/0000_enable_pgvector.sql` plus `drizzle/meta/_journal.json` and `drizzle/meta/0000_snapshot.json`. Hand-edit `drizzle/0000_enable_pgvector.sql` to contain EXACTLY:

    ```sql
    -- drizzle/0000_enable_pgvector.sql
    CREATE EXTENSION IF NOT EXISTS vector;
    ```

    Why `IF NOT EXISTS`: makes the migration idempotent at the SQL level. Re-running has no effect — supports the `scripts/probe-migration-idempotent.sh` validation gate (Plan 00 created the probe; Plan 05 runs it).

    If the executor cannot run `bun drizzle-kit generate --custom` because DATABASE_URL is unset (validation throws via env.ts), set `SKIP_ENV_VALIDATION=1` for this one-shot:

    ```bash
    SKIP_ENV_VALIDATION=1 bun drizzle-kit generate --custom --name=enable_pgvector
    ```

    Then unset `SKIP_ENV_VALIDATION` afterward. The `meta/` files (journal + snapshot) MUST be committed — they are versioned per Drizzle convention (PATTERNS.md §"`.gitignore`" lines 866-869: do NOT add drizzle/meta to .gitignore).

    Step C — Inspect the generated journal:

    ```bash
    cat drizzle/meta/_journal.json
    ```

    Expected: a JSON object with one entry tagging `0000_enable_pgvector`. If drizzle-kit emitted additional schema entries (it shouldn't — schema.ts does not exist yet), STOP and consult RESEARCH §6 for ordering. The custom migration must be tagged `0000` so Plan 03's auto-generated schema migration becomes `0001`.

    Step D — Verify Biome accepts the TS file: `bun check:format drizzle.config.ts && bun check:lint drizzle.config.ts && bun check:ts`. The SQL file is not subject to Biome.
  </action>
  <verify>
    <automated>test -f drizzle.config.ts &amp;&amp; test -f drizzle/0000_enable_pgvector.sql &amp;&amp; grep -F 'CREATE EXTENSION IF NOT EXISTS vector' drizzle/0000_enable_pgvector.sql &amp;&amp; grep -F 'schemaFilter: [&quot;public&quot;]' drizzle.config.ts &amp;&amp; grep -F 'from &quot;@/lib/env&quot;' drizzle.config.ts &amp;&amp; bun check:ts</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle.config.ts` exists at repo root.
    - `drizzle.config.ts` imports from `@/lib/env`: `grep -c 'from "@/lib/env"' drizzle.config.ts` returns 1.
    - `drizzle.config.ts` does NOT use `process.env.DATABASE_URL` directly: `grep -c 'process.env' drizzle.config.ts` returns 0.
    - `drizzle.config.ts` declares all required fields: `dialect: "postgresql"`, `schema: "./src/lib/db/schema.ts"`, `out: "./drizzle"`, `schemaFilter: ["public"]`, `strict: false`, `verbose: true` — each verifiable by grep with the exact form shown in the action.
    - `drizzle/0000_enable_pgvector.sql` exists and its content is exactly the comment line plus the `CREATE EXTENSION IF NOT EXISTS vector;` statement (whitespace tolerable): `grep -c 'CREATE EXTENSION IF NOT EXISTS vector' drizzle/0000_enable_pgvector.sql` returns 1.
    - `drizzle/meta/_journal.json` exists (auto-generated by drizzle-kit).
    - `drizzle/meta/0000_snapshot.json` exists (auto-generated).
    - `bun check:ts` passes (drizzle.config.ts type-checks).
    - `bun check:format drizzle.config.ts` reports no diffs.
    - No `vector(...)` column reference anywhere in repo: `grep -rn "vector(" --include='*.ts' src/` returns no matches (confirms CONTEXT D-12 — extension only, no columns).
  </acceptance_criteria>
  <done>
    drizzle-kit can introspect the project (`drizzle.config.ts` valid). The pgvector custom migration is committed as `0000_enable_pgvector.sql` and is idempotent. Plan 03 will run `bun db:generate --name=initial_schema` to produce `0001_initial_schema.sql` after schema.ts ships. The `schemaFilter: ['public']` documentation guard is in place; cross-schema FKs to `neon_auth.users_sync` will still be emitted by `generate` (intended).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-tool ↔ DATABASE_URL | drizzle-kit reads DATABASE_URL via env.ts; never logged, never inlined into committed files |
| Migration runner ↔ live DB | `drizzle-kit migrate` executes raw SQL against the configured DATABASE_URL; SQL is reviewable in `drizzle/*.sql` before commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05 | Information Disclosure | DATABASE_URL leaked into committed config or logs | mitigate | drizzle.config.ts reads via `import { env } from "@/lib/env"` (server-only var per T3-Env); no `process.env` access; `verbose: true` logs SQL not credentials. |
| T-1-06 | Tampering | malformed pgvector SQL applied to production | mitigate | The 0000 migration contains exactly `CREATE EXTENSION IF NOT EXISTS vector;` — review-able diff at `drizzle/0000_enable_pgvector.sql`. `IF NOT EXISTS` makes re-runs idempotent (no destructive ALTER). |
| T-1-07 | Denial of Service | drizzle-kit `push` accidentally run against production | accept | CONTEXT D-13 explicitly forbids `push` outside local dev. The `db:push` script is included for parity but the production build pipeline (Plan 05) uses `db:migrate` only. Documented in Plan 05's CONTRIBUTING update. |
| T-1-08 | Elevation of Privilege | extension creation requires Postgres superuser permissions | accept | Neon Postgres grants the project owner sufficient privileges to install allow-listed extensions including pgvector [CITED: neon.com/docs/extensions/pg_vector]. Local Docker pgvector image (CONTEXT D-03 fallback) ships with the extension pre-installed. No additional privilege escalation needed. |
</threat_model>

<verification>
After both tasks complete:
- `bunx drizzle-kit --version` returns 0.31.x
- `bun check:ts` passes (config file is valid TS)
- `bun check:format drizzle.config.ts package.json` reports no diffs
- `drizzle/0000_enable_pgvector.sql` is the FIRST entry in `drizzle/meta/_journal.json`
- No schema migration exists yet — `drizzle/0001_*.sql` is absent (Plan 03 creates it)
- `package.json` build script is still `next build` exactly (Plan 05 changes this)
</verification>

<success_criteria>
- INFRA-07 (pgvector extension scaffolded for Plan 05's apply step) — migration file exists; actual `bun db:migrate` apply happens in Plan 05 against the live Neon dev branch.
- INFRA-09 (tooling half) — drizzle-kit installed, db:* scripts wired, drizzle.config.ts uses env.ts (no hardcoded secrets).
- Wave 1 closes when Plan 01 + Plan 02 both complete; Wave 2 (schema + clients) can start.
- `bun db:generate --name=initial_schema` (Plan 03's first step) is now executable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation/01-02-SUMMARY.md` documenting:
- Pinned versions installed (`drizzle-orm`, `drizzle-kit`)
- drizzle.config.ts shape (dialect, schema, out, schemaFilter, env-driven dbCredentials)
- Custom migration created: `drizzle/0000_enable_pgvector.sql` with idempotent `CREATE EXTENSION IF NOT EXISTS vector;`
- Journal entry confirmed (`drizzle/meta/_journal.json` lists 0000_enable_pgvector)
- Confirmed: package.json `build` script untouched (Plan 05 owns it)
</output>
</content>
</invoke>
---
phase: 01-database-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - "01-00"
  - "01-01"
  - "01-02"
  - "01-03"
  - "01-04"
files_modified:
  - package.json
  - CONTRIBUTING.md
  - docker-compose.yml
  - tests/db/setup.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-07
  - INFRA-08
  - INFRA-09
tags:
  - migration-apply
  - vercel-build
  - smoke-tests
  - contributing
  - blocking

must_haves:
  truths:
    - "`bun db:migrate` has been run against the live Neon dev branch and applied BOTH 0000_enable_pgvector.sql AND 0001_initial_schema.sql"
    - "Running `bun db:migrate` a SECOND time prints `0 migrations to apply` (or equivalent) — proves idempotency"
    - "`scripts/probe-pgvector.ts` exits 0 against the live DB (pgvector extension actually present in pg_extension)"
    - "`scripts/probe-migration-idempotent.sh` exits 0 (full automation gate for INFRA-09)"
    - "`package.json` `build` script equals `bun db:migrate && next build` exactly — Vercel build runs migrations BEFORE next build"
    - "Wave 0 schema tests (users, topics, votes.unique) flip GREEN with PHASE_1_DB_READY=1; tests/db/setup.ts truncate fixture activates"
    - "`bun dev` starts cleanly against the live DATABASE_URL — connectivity smoke test"
    - "`bun run build` completes successfully end-to-end (Vercel-equivalent migrate + next build)"
    - "CONTRIBUTING.md documents Neon dev-branch onboarding (canonical) AND local Docker pgvector fallback"
    - "docker-compose.yml exists with `pgvector/pgvector:pg17` for offline contributors"
    - "User checkpoint confirms Vercel preview deploy succeeded with all env vars wired"
  artifacts:
    - path: "package.json"
      provides: "build script prepended with `bun db:migrate` for Vercel + local parity"
      contains: "bun db:migrate && next build"
    - path: "CONTRIBUTING.md"
      provides: "Augmented with Phase 1 sections: Neon dev branch (canonical), Docker fallback, db:* scripts, env setup"
      contains: "DATABASE_URL"
    - path: "docker-compose.yml"
      provides: "Offline-contributor fallback: Postgres 17 + pgvector image"
      contains: "pgvector/pgvector:pg17"
    - path: "tests/db/setup.ts"
      provides: "Activated TRUNCATE fixture (Wave 0 created the gated stub; this plan removes the gate by setting PHASE_1_DB_READY)"
      contains: "TRUNCATE TABLE"
  key_links:
    - from: "package.json scripts.build"
      to: "drizzle-kit migrate"
      via: "`bun db:migrate &&` prepended to `next build`"
      pattern: "bun db:migrate && next build"
    - from: "Vercel build pipeline"
      to: "Neon Postgres"
      via: "DATABASE_URL injected as Build & Runtime env var; bun db:migrate executes before next build"
      pattern: "Vercel env vars"
    - from: "scripts/probe-migration-idempotent.sh"
      to: "drizzle __drizzle_migrations table"
      via: "second db:migrate detects already-applied entries, prints `0 migrations to apply`"
      pattern: "0 migrations to apply|already applied|nothing to migrate"
---

<objective>
**[BLOCKING]** This plan closes Phase 1. It applies the migrations against the live Neon dev branch, wires `bun db:migrate` into the Vercel build pipeline, runs every smoke probe end-to-end, flips Wave 0's remaining RED tests to GREEN, ships `CONTRIBUTING.md` + `docker-compose.yml` for the local-dev story, and gates on a user-verified Vercel preview deploy.

Per CONTEXT D-13: `drizzle-kit migrate` (NOT `push`) is the production-grade verification gate. Per CONTEXT D-14: Vercel deploys run migrations as part of the build step and fail loudly on error. Per RESEARCH §6 ordering: `0000_enable_pgvector.sql` runs BEFORE `0001_initial_schema.sql` (drizzle-kit's tag-ordered journal guarantees this).

This plan is the difference between "Phase 1 LOOKS done because the schema TypeScript compiles" and "Phase 1 IS done because the schema is actually live in the DB and Vercel deploys cleanly." Phase verification cannot pass without it — types come from `schema.ts`, not the live DB, so build/typecheck pass without migration (false-positive risk per planning context's `<schema_push_requirement>`).

Purpose: Ship-grade close-out for Phase 1. Every requirement INFRA-01..03, INFRA-07, INFRA-08, INFRA-09 is verified end-to-end. Plan 06 (Phase 2 — Magic-Link Auth) can begin with confidence that the schema is real, the build pipeline works, and the contributor story is documented.
Output: build script wired, migration applied, smoke probes green, CONTRIBUTING + docker-compose committed, Wave 0 schema tests green, user-verified Vercel preview deploy.
</objective>

<execution_context>
@/Users/alec/Projects/unsere-news/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alec/Projects/unsere-news/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-database-foundation/01-CONTEXT.md
@.planning/phases/01-database-foundation/01-RESEARCH.md
@.planning/phases/01-database-foundation/01-PATTERNS.md
@.planning/phases/01-database-foundation/01-VALIDATION.md
@.planning/phases/01-database-foundation/01-00-PLAN.md
@.planning/phases/01-database-foundation/01-01-PLAN.md
@.planning/phases/01-database-foundation/01-02-PLAN.md
@.planning/phases/01-database-foundation/01-03-PLAN.md
@.planning/phases/01-database-foundation/01-04-PLAN.md
@CONTRIBUTING.md
@package.json
@.env.example
@drizzle.config.ts
@drizzle/0000_enable_pgvector.sql
@drizzle/0001_initial_schema.sql
@src/lib/db/schema.ts
@src/lib/db/index.ts
@src/lib/qstash.ts
@src/lib/env.ts

<interfaces>
By Wave 3 (when this plan starts), the following have been delivered by upstream plans:

- `src/lib/env.ts` (Plan 01) — validates DATABASE_URL, NEON_AUTH_*, QSTASH_*, ANTHROPIC_API_KEY at module load
- `.env.example` (Plan 01) — augmented with NEON_AUTH_*, BETTER_AUTH_* removed
- `drizzle.config.ts` (Plan 02) — env-driven, schemaFilter: ['public']
- `drizzle/0000_enable_pgvector.sql` (Plan 02) — `CREATE EXTENSION IF NOT EXISTS vector;`
- `drizzle/0001_initial_schema.sql` (Plan 03) — generated from schema.ts; cross-schema FK to neon_auth.users_sync; composite unique on votes
- `drizzle/meta/_journal.json` (Plans 02 + 03) — lists 0000 and 0001 in order
- `src/lib/db/schema.ts` + `src/lib/db/index.ts` (Plan 03) — typed db client
- `src/lib/qstash.ts` (Plan 04) — lazy QStash client
- `package.json` scripts: `db:generate`, `db:migrate`, `db:push`, `db:studio`, `test`, `test:db`, `test:watch`. `build` is still `next build` exactly (this plan changes that).
- Wave 0 tests: 8 test files + 2 probe scripts; the env tests + qstash test are GREEN; the schema/db tests are RED (gated on `PHASE_1_DB_READY=1` — this plan flips them).

CONTEXT D-14: build must fail LOUDLY if migration fails. The `&&` chain (`bun db:migrate && next build`) gives that semantic — if migrate exits non-zero, next build never runs and Vercel marks deploy failed.

Existing CONTRIBUTING.md (read first; this plan augments, never replaces):
- Documents the GSD planning workflow at the top
- Has sections covering project setup
- This plan adds a "Database Setup" section (Phase 1 deliverable per CONTEXT D-03)

External dependencies for the user checkpoint:
- A Neon project with Auth enabled
- A Vercel project linked to the GitHub repo
- The Neon-Vercel integration installed (auto-provisions DATABASE_URL + NEON_AUTH_* on preview branches per RESEARCH §10)
- QSTASH_* env vars set in Vercel project settings (placeholder values acceptable in v0.2)

If user does NOT have a Vercel/Neon account ready, the user checkpoint can be skipped with explicit user approval, but the local `bun run build` smoke test (which exercises `bun db:migrate && next build` end-to-end against the dev branch) remains mandatory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 05-1: Apply migrations to Neon dev branch + run pgvector + idempotency probes</name>
  <files>tests/db/setup.ts</files>
  <read_first>
    - `01-RESEARCH.md` §6 lines 456-461 (Step C — apply locally: `bun db:migrate`)
    - `01-RESEARCH.md` §6 lines 487-493 (idempotency: `__drizzle_migrations` table)
    - `01-RESEARCH.md` §11 lines 837-869 (probe specs verbatim)
    - `01-RESEARCH.md` §14 Pitfall 1 lines 988-996 (verify users.id text in live DB, not just schema.ts)
    - `01-VALIDATION.md` §"Per-Task Verification Map" lines 42-55 (every test ID — this plan turns the remaining REDs to GREEN)
    - `01-VALIDATION.md` §"Sampling Rate" lines 33-37 (after every plan wave: full suite + probe)
    - `01-CONTEXT.md` D-13 (push is dev-only, NEVER the verification gate; migrate is the gate)
    - `01-CONTEXT.md` D-03 (Neon dev branch is the canonical local DB)
    - `tests/db/setup.ts` (Plan 00 — currently has `PHASE_1_DB_READY` gate; this task flips the gate)
  </read_first>
  <action>
    Step A — Confirm DATABASE_URL is wired locally. The user must have populated `.env.local` from a Neon dev branch via `bunx vercel env pull .env.local` OR by manual copy from Neon Console. Verify by reading (do NOT log the value):

    ```bash
    test -f .env.local && grep -q '^DATABASE_URL=postgresql://' .env.local || {
      echo "FAIL: .env.local missing DATABASE_URL — populate from Neon Console or run 'bunx vercel env pull .env.local'";
      exit 1;
    }
    ```

    If FAIL: STOP. The user must populate DATABASE_URL before this plan can proceed. Do NOT use a placeholder URL — migration apply needs a real, reachable Postgres.

    Step B — Apply migrations:

    ```bash
    bun db:migrate
    ```

    Expected output (approximate; exact text depends on drizzle-kit version):
    ```
    Reading config file '/.../drizzle.config.ts'
    Using 'postgres' driver for database querying
    [✓] applied migration: 0000_enable_pgvector
    [✓] applied migration: 0001_initial_schema
    ```

    On success, the live DB now has:
    - `vector` extension enabled in `pg_extension`
    - `topic_status` enum in `public`
    - `users`, `topics`, `votes` tables in `public`
    - FK from `users.id` → `neon_auth.users_sync(id)`
    - composite unique constraint on votes
    - `__drizzle_migrations` table tracking the two applied entries

    On failure:
    - "permission denied to create extension" — the Neon role lacks superuser. Contact Neon support OR use a different role with elevated privileges. Pgvector is on Neon's allow-list per RESEARCH §14 Pitfall 8 footnote — the typical issue is connecting as a non-owner role.
    - "neon_auth.users_sync does not exist" — Neon Auth is NOT enabled on the project. Enable it in Neon Console → Auth → Setup BEFORE retrying. The FK in 0001 will fail without the target table existing.
    - Any other error: print the full output, do NOT proceed; consult RESEARCH §14 Pitfalls.

    Step C — Verify pgvector extension is live. Run the Plan 00 probe:

    ```bash
    bun run scripts/probe-pgvector.ts
    ```

    Expected output: `PASS: pgvector extension enabled` and exit 0. If it prints `FAIL` and exits 1: pgvector did NOT install. Check Neon Console → Database → Extensions to enable manually if Neon's auto-allow-list does not cover the role; or re-apply the 0000 migration with a superuser role.

    Step D — Verify idempotency. Run the Plan 00 probe:

    ```bash
    bash scripts/probe-migration-idempotent.sh
    ```

    The script runs `bun db:migrate` twice; the second run must print one of: `already applied`, `0 migrations to apply`, or `nothing to migrate`. Expected output: `PASS: migration is idempotent` and exit 0.

    If the second run actually applies migrations again — STOP. Migration is NOT idempotent. Most likely cause: the executor accidentally ran `db:push` (which doesn't track journal) at some point and corrupted `__drizzle_migrations`. Drop the offending tables manually and re-run from Step B. CONTEXT D-13 explicitly forbids `push` for verification — do NOT use it here.

    Step E — Verify users.id is `text` in the live DB (not just in schema.ts). Run via psql or direct SQL probe:

    ```bash
    bun -e "
    import('@/lib/db').then(async ({ db }) => {
      const { sql } = await import('drizzle-orm');
      const r = await db.execute(sql\`
        SELECT data_type FROM information_schema.columns
        WHERE table_schema='public' AND table_name='users' AND column_name='id'
      \`);
      const t = r.rows[0]?.data_type;
      if (t !== 'text') { console.error('FAIL: users.id is', t, 'expected text'); process.exit(1); }
      console.log('PASS: users.id is text');
    });
    "
    ```

    Expected: `PASS: users.id is text`. If FAIL: schema.ts is wrong; reverse-apply the migration, fix Plan 03's schema.ts, re-generate, and re-apply.

    Step F — Activate the truncate fixture in `tests/db/setup.ts`. Plan 00 gated truncation behind `PHASE_1_DB_READY=1`. Now that the schema is live, EITHER:

    - Option A (recommended): export `PHASE_1_DB_READY=1` in the test environment via `vitest.config.ts` `env` block OR via `.env.test` (which Vitest auto-loads). Do this only if a `.env.test` file is gitignored (it is — `.env*` is in `.gitignore`).
    - Option B (simpler): edit `tests/db/setup.ts` to remove the `dbReady` gate now that the schema exists. This makes the fixture unconditional, which is correct after this plan since the migration is permanent.

    Choose Option B (simpler, no test-env coupling). Edit `tests/db/setup.ts` to:

    ```typescript
    import { afterEach, beforeAll } from "vitest";
    import { sql } from "drizzle-orm";

    import { db } from "@/lib/db";

    /**
     * Shared test fixture for Phase 1.
     *
     * Plan 05 activated this fixture once the schema landed live in the DB.
     * Before each test:  no setup — tests seed their own users/topics rows.
     * After each test:   TRUNCATE all three tables (CASCADE) for isolation.
     *
     * Note: tests that exercise FK to neon_auth.users_sync (e.g., votes.unique.test.ts)
     * must seed a corresponding neon_auth.users_sync row first OR run against a Neon dev
     * branch where Neon Auth has at least one synced user. For local Docker fallback,
     * such tests should be skipped via vitest's it.skipIf or run only in CI.
     */

    beforeAll(async () => {
      // Future hook: optionally seed neon_auth.users_sync stub for FK satisfaction.
    });

    afterEach(async () => {
      await db.execute(sql`TRUNCATE TABLE votes, topics, users CASCADE`);
    });
    ```

    Verify Biome: `bun check:format tests/db/setup.ts`.

    Step G — Run the schema tests. They MUST flip GREEN now that the migration applied:

    ```bash
    bun test tests/db
    ```

    Expected output: `users.schema`, `topics.schema`, and `votes.unique` tests PASS. Total: ~3-5 tests passing.

    If `tests/db/votes.unique.test.ts` fails with FK violation: the test seeds a `users` row but the user is not in `neon_auth.users_sync`. Either (a) seed a real user via Neon Auth's signup flow against the dev branch first, OR (b) update `tests/db/setup.ts`'s `beforeAll` to insert a stub `neon_auth.users_sync` row matching the test fixture's userId. Option (b) requires `INSERT INTO neon_auth.users_sync (id, raw_json) VALUES ('test-user-id-stub', '{}')` — verify Neon Auth doesn't error on direct insert.

    For Phase 1 acceptance: it is sufficient if the votes.unique test asserts the unique-constraint shape via DDL introspection (no insert needed). If the executor cannot solve the FK seed problem in 30 minutes, replace the test body with a DDL introspection check:

    ```typescript
    it("has composite unique constraint votes_user_topic_week_unique", async () => {
      const { db } = await import("@/lib/db");
      const { sql } = await import("drizzle-orm");
      const rows = await db.execute(sql`
        SELECT conname FROM pg_constraint
        WHERE conrelid = '"votes"'::regclass AND contype = 'u'
      `);
      const names = rows.rows.map((r) => r.conname);
      expect(names).toContain("votes_user_topic_week_unique");
    });
    ```

    This DDL-only check satisfies INFRA-03 (the constraint exists and is correctly named) without requiring auth-schema seeding. Editor's discretion on the trade-off — document the choice in the SUMMARY.
  </action>
  <verify>
    <automated>bun run scripts/probe-pgvector.ts &amp;&amp; bash scripts/probe-migration-idempotent.sh &amp;&amp; bun test tests/db</automated>
  </verify>
  <acceptance_criteria>
    - `bun db:migrate` applied successfully against the live Neon dev branch (manually verified by executor in Step B output).
    - `scripts/probe-pgvector.ts` exits 0 with `PASS: pgvector extension enabled`.
    - `scripts/probe-migration-idempotent.sh` exits 0 with `PASS: migration is idempotent`.
    - `users.id` data_type in live DB is `text` (NOT `uuid`) — verified by Step E one-liner.
    - `tests/db/setup.ts` no longer gates on `PHASE_1_DB_READY` — TRUNCATE runs unconditionally after each test.
    - `bun test tests/db` passes all schema tests (users + topics + votes.unique). At least 3 tests must report passing.
    - `bun check:format tests/db/setup.ts` reports no diffs.
    - `bun check:ts` passes.
    - The `__drizzle_migrations` table in the live DB lists exactly 2 entries: `0000_enable_pgvector` and `0001_initial_schema` (manually verifiable; not a hard gate but inspect once).
  </acceptance_criteria>
  <done>
    The schema is live in Neon. pgvector extension is enabled. Idempotency proven. Wave 0 schema tests flipped from RED to GREEN. INFRA-01, INFRA-02, INFRA-03, INFRA-07 verified end-to-end. Wave 0's setup fixture is now active and unconditional.
  </done>
</task>

<task type="auto">
  <name>Task 05-2: Wire `bun db:migrate` into Vercel build script + verify `bun run build` end-to-end</name>
  <files>package.json</files>
  <read_first>
    - `01-RESEARCH.md` §6 lines 463-477 (Vercel deploy wiring rationale: build, not start)
    - `01-RESEARCH.md` §14 Pitfall 2 lines 998-1006 (don't put migrations in start)
    - `01-PATTERNS.md` Convention 5 lines 150-182 (script naming + `&&` chain pattern; `build` modification shown)
    - `01-PATTERNS.md` §"package.json — modify" lines 768-806 (full diff including build line)
    - `01-CONTEXT.md` D-14 (Vercel build step; loud-fail on migration error)
    - existing `package.json` `build` script (currently `next build` — this task changes it)
  </read_first>
  <action>
    Step A — Modify `package.json` `build` script. Change EXACTLY one line:

    Before:
    ```jsonc
    "build": "next build",
    ```

    After:
    ```jsonc
    "build": "bun db:migrate && next build",
    ```

    The `&&` chain mirrors `check:all`'s style. If `bun db:migrate` exits non-zero, `next build` never runs and the deploy fails loudly per CONTEXT D-14.

    Why `build`, not `start` (RESEARCH §6):
    - `start` runs per request/instance on Vercel — multiple parallel containers race on migration locks.
    - `build` runs once per deployment, sequentially, and FAILS THE DEPLOY on migration error.
    - DATABASE_URL must be available at build time → set in Vercel project settings as **Build & Runtime** env var (not Runtime-only). User checkpoint in Task 05-4 confirms this.

    Why not the programmatic `migrate()` from drizzle-orm (RESEARCH §6):
    - `drizzle-kit migrate` (CLI) reads `drizzle.config.ts` and the journal automatically — fewer lines, single source of truth.
    - The programmatic API is overkill for v0.2.

    Step B — Verify locally end-to-end. With DATABASE_URL set:

    ```bash
    bun run build
    ```

    Expected behavior:
    1. `bun db:migrate` runs — already-applied entries print "0 migrations to apply" (idempotent thanks to Task 05-1).
    2. `next build` runs — produces `.next/` output.
    3. Exit code 0.

    If `next build` fails with bundling errors mentioning `WebSocket`, `bufferutil`, or `utf-8-validate`: PATTERNS.md §next.config.ts lines 873-892 documented this conditional. Add `serverExternalPackages: ["@neondatabase/serverless"]` to `next.config.ts` (preserve `withNextIntl(nextConfig)` wrap):

    ```typescript
    import type { NextConfig } from "next";
    import createNextIntlPlugin from "next-intl/plugin";

    const withNextIntl = createNextIntlPlugin();

    const nextConfig: NextConfig = {
      serverExternalPackages: ["@neondatabase/serverless"],
    };

    export default withNextIntl(nextConfig);
    ```

    Add this only if `bun run build` complains. Next.js 16 may handle Neon's serverless package automatically (RESEARCH §3 noted this is conditional). Do NOT touch `next.config.ts` unless the build forces you to.

    Step C — Re-run `bun check:all` to confirm all gates still pass:

    ```bash
    bun check:all
    ```

    All four sub-checks (imports, format, lint, ts) must exit 0.

    Step D — Re-run the full test suite to confirm no regression:

    ```bash
    bun test
    ```

    Expected: all tests GREEN (env, env.example, qstash, users.schema, topics.schema, votes.unique). Count: ~7-9 tests passing total.
  </action>
  <verify>
    <automated>grep -F '"build": "bun db:migrate && next build"' package.json &amp;&amp; bun check:all &amp;&amp; bun run build &amp;&amp; bun test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `build` script equals exactly `"bun db:migrate && next build"`: `grep -F '"build": "bun db:migrate && next build"' package.json` matches.
    - `bun run build` exits 0 (full Vercel-equivalent: migrate idempotent + next build succeeds).
    - `bun check:all` exits 0 (no regressions in lint/format/imports/ts).
    - `bun test` exits 0 (all Phase 1 tests green).
    - If `next.config.ts` was modified: it still wraps with `withNextIntl(nextConfig)` (Convention 6); change is ONLY adding `serverExternalPackages` inside the `nextConfig` object.
    - `package.json` formatting clean: `bun check:format package.json` reports no diffs.
  </acceptance_criteria>
  <done>
    Vercel build pipeline is wired. Migrations run BEFORE next build. Local `bun run build` proves the full pipeline works end-to-end against the live DB. All Phase 1 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 05-3: Ship CONTRIBUTING.md database-setup section + docker-compose.yml fallback</name>
  <files>CONTRIBUTING.md, docker-compose.yml</files>
  <read_first>
    - existing `CONTRIBUTING.md` (full file — augment, never replace)
    - `01-RESEARCH.md` §10 lines 730-803 (Local Dev Story; Neon dev branch canonical, Docker fallback)
    - `01-RESEARCH.md` §10 lines 757-773 (docker-compose.yml verbatim spec)
    - `01-CONTEXT.md` D-03 (Neon dev branch canonical; Docker documented fallback)
    - `01-CONTEXT.md` `<deferred>` section (full self-host docs are out of v0.x scope — only basic fallback path here)
    - `01-PATTERNS.md` Watchpoint 7 lines 994 (Neon dev branch vs Docker for tests strategy)
  </read_first>
  <action>
    Step A — Create `docker-compose.yml` at repo root with EXACTLY this content (verbatim from RESEARCH §10 lines 757-773):

    ```yaml
    services:
      postgres:
        image: pgvector/pgvector:pg17
        container_name: kebab-news-postgres
        environment:
          POSTGRES_USER: kebab
          POSTGRES_PASSWORD: kebab
          POSTGRES_DB: kebab_news
        ports:
          - "5432:5432"
        volumes:
          - kebab_pg_data:/var/lib/postgresql/data
    volumes:
      kebab_pg_data:
    ```

    Note: the password `kebab` is intentionally weak. This is a LOCAL-ONLY image for offline contributors. The DATABASE_URL contributors set is `postgresql://kebab:kebab@localhost:5432/kebab_news?sslmode=disable`. Document explicitly that this image does NOT support `neon_auth.users_sync` (it's a vanilla Postgres + pgvector, no Neon Auth) — tests that exercise the FK to `neon_auth.users_sync` will fail or must be skipped in this mode.

    Step B — Augment `CONTRIBUTING.md`. Read the existing file first (it has GSD workflow content at the top). Add a new section AFTER the existing introductory content. Use this template (copy-friendly):

    ```markdown
    ## Database Setup (v0.2)

    kebab.news uses Neon Postgres with Drizzle ORM. There are two paths to a working local database — the canonical path (Neon dev branch) and a fallback (local Docker).

    ### Canonical path: Neon dev branch

    Recommended for all contributors with internet access.

    1. Create a free Neon account at https://neon.tech
    2. Create a Neon project for kebab-news
    3. Enable Neon Auth in the project (Console → Auth → Setup) — required for the `neon_auth.users_sync` schema referenced by `public.users.id` FK
    4. Install the Neon-Vercel marketplace integration on your Vercel project (free tier sufficient)
    5. Pull dev-branch env vars locally:
       ```bash
       bunx vercel env pull .env.local
       ```
       This populates `.env.local` with `DATABASE_URL` (dev branch), `NEON_AUTH_BASE_URL`, `NEON_AUTH_COOKIE_SECRET`, and any other configured vars.
    6. Apply migrations:
       ```bash
       bun db:migrate
       ```
    7. Start the dev server:
       ```bash
       bun dev
       ```

    PR-preview deploys auto-create isolated Neon branches via the marketplace integration — no extra setup required.

    ### Fallback path: Local Docker (offline / airgapped)

    Use this only if you cannot reach Neon. Limitation: this image does NOT have `neon_auth.users_sync`, so any test that exercises the `users.id → neon_auth.users_sync(id)` FK will fail. Such tests must be skipped or run only in CI against a Neon dev branch.

    1. Start the Postgres + pgvector container:
       ```bash
       docker compose up -d postgres
       ```
       (Image: `pgvector/pgvector:pg17` — Postgres 17 + pgvector pre-installed.)

    2. Add to `.env.local`:
       ```
       DATABASE_URL=postgresql://kebab:kebab@localhost:5432/kebab_news?sslmode=disable
       NEON_AUTH_BASE_URL=http://localhost:9999/auth
       NEON_AUTH_COOKIE_SECRET=00000000000000000000000000000000
       ```
       (Last two are placeholders — Phase 2 auth flows will not work in Docker mode; use Neon dev branch for auth-touching work.)

    3. Apply migrations:
       ```bash
       bun db:migrate
       ```
       The 0001 schema migration will FAIL because `neon_auth.users_sync` does not exist. To proceed locally, either (a) skip the 0001 migration and only run 0000_enable_pgvector for SQL exploration, or (b) manually create a stub `neon_auth.users_sync` table with `id text PRIMARY KEY, raw_json jsonb` columns. Option (b) is documented for advanced contributors only.

    ### Database scripts

    | Script | Purpose |
    |--------|---------|
    | `bun db:generate` | Generate a new SQL migration from `src/lib/db/schema.ts` changes |
    | `bun db:migrate` | Apply pending migrations against `DATABASE_URL` (idempotent — safe to re-run) |
    | `bun db:push` | LOCAL DEV ONLY — push schema directly without a migration file. NEVER use against production |
    | `bun db:studio` | Open Drizzle Studio (a web UI to browse the DB) |

    ### Tests

    | Script | Purpose |
    |--------|---------|
    | `bun test` | Run all tests once (vitest run) |
    | `bun test:db` | Run only DB-touching tests (`tests/db/`) |
    | `bun test:watch` | Watch mode for active development |

    Tests use the live `DATABASE_URL` and `TRUNCATE` tables between tests for isolation. Set `PHASE_1_DB_READY=1` is no longer needed — the fixture is unconditional after Phase 1 closed.

    ### Schema constraints (don't break these)

    - `public.users.id` is **`text`** (not `uuid`) — matches `neon_auth.users_sync.id`. Do NOT change this without changing the Neon Auth-managed type (which Drizzle controls via `usersSync` from `drizzle-orm/neon`).
    - Migration 0000 is `enable_pgvector` (custom; runs before any schema migration). Migration 0001 is `initial_schema` (auto-generated). Never put `CREATE EXTENSION` in a generated schema migration.
    - `votes` has a composite unique on `(user_id, topic_id, week_bucket)` — Phase 5's weekly budget logic relies on this.
    ```

    Insert this section in `CONTRIBUTING.md` at a sensible place — recommended position: AFTER any existing "Getting Started" or installation section, BEFORE deeper contributor topics. If there are no such sections, append to end of file with a single blank line separator.

    DO NOT delete or rewrite existing CONTRIBUTING.md content. The "augment, never replace" rule (Convention 7 from PATTERNS.md) applies to all docs in this phase.

    Step C — Format check (CONTRIBUTING.md is markdown; Biome formats it):

    ```bash
    bun check:format CONTRIBUTING.md docker-compose.yml
    ```

    Should report no diffs. If Biome doesn't handle YAML, run `cat docker-compose.yml | head` and confirm 2-space indent + LF line endings manually.

    Step D — Verify the docker-compose.yml is syntactically valid:

    ```bash
    docker compose config --quiet
    ```

    Exit 0 means valid. If `docker` is not installed on the executor's machine, skip this validation and document in SUMMARY that it requires manual verification by a contributor with Docker available.
  </action>
  <verify>
    <automated>test -f docker-compose.yml &amp;&amp; grep -F 'pgvector/pgvector:pg17' docker-compose.yml &amp;&amp; grep -F 'Database Setup (v0.2)' CONTRIBUTING.md &amp;&amp; grep -F 'Neon dev branch' CONTRIBUTING.md &amp;&amp; grep -F 'bun db:migrate' CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists at repo root with `pgvector/pgvector:pg17` image, `kebab` user, `kebab_news` database, port 5432, named volume `kebab_pg_data`.
    - `CONTRIBUTING.md` contains a `## Database Setup (v0.2)` section.
    - Section documents BOTH the Neon dev-branch path AND the Docker fallback.
    - Section lists the `db:*` script aliases with one-line descriptions each.
    - Section includes the schema constraint guard ("`public.users.id` is **`text`** (not `uuid`)") to prevent future executors from breaking the cross-schema FK.
    - Existing CONTRIBUTING.md content (anything that was already there) is unchanged: `git diff` shows only additions, never deletions of existing lines.
    - `bun check:format CONTRIBUTING.md docker-compose.yml` reports no diffs (or YAML/MD files outside Biome's reach pass manual inspection).
    - If Docker available: `docker compose config --quiet` exits 0.
  </acceptance_criteria>
  <done>
    Contributors have a reproducible local-dev story. Neon dev branch is documented as the canonical path. Docker fallback is documented with explicit caveats (no Neon Auth schema). The schema constraint guard ("users.id is text, not uuid") is committed in docs to prevent the most-likely future regression.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 05-4: User checkpoint — Vercel preview deploy succeeds with all env vars wired</name>
  <what-built>
    Plans 00-05 have delivered the entire Phase 1 deliverable:
    - vitest installed; 8 test files green; 2 probe scripts green
    - env.ts (validated server/client split); .env.example augmented (NEON_AUTH_*, no BETTER_AUTH_*)
    - drizzle.config.ts; pgvector custom migration (0000) + auto-generated initial schema (0001)
    - src/lib/db/schema.ts (users, topics, votes, topicStatusEnum, usersSync re-export)
    - src/lib/db/index.ts (typed db client via @neondatabase/serverless HTTP)
    - src/lib/qstash.ts (lazy QStash client; no network on construct)
    - bun db:migrate applied locally; pgvector + idempotency probes pass
    - bun run build runs migrate + next build end-to-end
    - CONTRIBUTING.md augmented; docker-compose.yml fallback shipped

    The remaining Phase 1 success criterion (per ROADMAP §Phase 1 #5) is: **"Vercel deployment succeeds with all env vars wired; Upstash QStash client initializes (env present, not yet invoked)."** This is by definition human-only — only the user can authenticate to Vercel and verify the deploy.
  </what-built>
  <how-to-verify>
    1. Push the current branch to the remote:
       ```bash
       git push origin <current-branch>
       ```
       (If working on a fresh branch, `git push -u origin <branch>` to set upstream.)

    2. Open the Vercel dashboard for the kebab-news project.

    3. Confirm the following env vars are set in Vercel project settings (Build & Runtime scope):
       - `DATABASE_URL` (auto-provided by Neon-Vercel integration if installed)
       - `NEON_AUTH_BASE_URL` (auto-provided by Neon Auth if enabled)
       - `NEON_AUTH_COOKIE_SECRET` (manually set; ≥32 chars; generate with `openssl rand -base64 32`)
       - `QSTASH_URL` (default `https://qstash.upstash.io`)
       - `QSTASH_TOKEN` (placeholder OK in v0.2 — required only when QStash is invoked in Phase 6+)
       - `QSTASH_CURRENT_SIGNING_KEY`, `QSTASH_NEXT_SIGNING_KEY` (placeholder OK)
       - `ANTHROPIC_API_KEY` (placeholder OK in v0.2)
       - `NEXT_PUBLIC_APP_URL` (e.g. `https://kebab-news.vercel.app`)

    4. Wait for the preview deploy to finish (typically 1-3 minutes).

    5. Check the Vercel build log:
       - Look for `Reading config file '/.../drizzle.config.ts'` — confirms `bun db:migrate` ran
       - Look for `0 migrations to apply` (since migrations already applied locally to the SAME dev branch) OR `[✓] applied migration: 0001_initial_schema` (if Vercel uses a fresh branch)
       - Confirm `next build` completes after `bun db:migrate`

    6. Visit the preview URL. The landing page should render without DB errors. Open the browser console — no client-side warnings about missing env vars.

    7. (Optional but recommended) Run `bunx vercel env pull .env.preview` locally and confirm the file contains DATABASE_URL, NEON_AUTH_BASE_URL, NEON_AUTH_COOKIE_SECRET — proves env-var wiring.

    Expected outcomes:
    - Preview deploy: GREEN (no failed status)
    - Build log: includes `bun db:migrate` step before `next build`
    - Preview URL: serves the landing page
    - QStash client: initialized in the build (would be a runtime error if `@/lib/qstash` import threw — verify by reading build log)

    If preview deploy FAILS:
    - "DATABASE_URL is not defined" → Vercel env var missing; add it in project settings.
    - "permission denied to create extension" → role lacks pgvector privilege; check Neon role configuration.
    - "neon_auth.users_sync does not exist" → Neon Auth not enabled on production branch; enable in Neon Console.
    - Any other error: copy the failing build-log section and respond with "issues" + the log excerpt.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the Vercel preview deploy succeeded and the preview URL serves the landing page.

    Type "skip" if you do not have a Vercel/Neon account ready (acceptable per the planning context — the local `bun run build` smoke test in Task 05-2 covers most of the pipeline). The Phase 1 SUMMARY will document which path was taken.

    Otherwise, describe the issue (e.g., "Vercel build failed with: <error>") and the executor will diagnose before resuming.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local `bun db:migrate` ↔ live Neon dev branch | Executor's local environment writes DDL to a real cloud database; mistakes are reversible only by manual SQL or branch reset |
| Vercel build ↔ Neon production branch | Build runs `bun db:migrate` against production DATABASE_URL — failure aborts deploy (CONTEXT D-14 loud-fail) |
| docker-compose.yml ↔ committed credentials | The `kebab:kebab` user/password is documented as local-only; the README/CONTRIBUTING wording must make this clear so contributors don't reuse it elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-18 | Tampering | parallel Vercel instances race on `drizzle-kit migrate` | mitigate | `bun db:migrate` wired into `build` (runs once per deploy, sequential) NOT `start` (per-instance, parallel). RESEARCH §14 Pitfall 2. |
| T-1-19 | Information Disclosure | DATABASE_URL printed in build logs | mitigate | drizzle-kit's default verbose level prints SQL but not credentials. CI/Vercel's secret-redaction layer scrubs known-secret patterns. Do NOT add `console.log(env.DATABASE_URL)` anywhere — would leak in build log. ASVS V14. |
| T-1-20 | Information Disclosure | committed docker-compose.yml password reused in production | mitigate | CONTRIBUTING.md text explicitly notes `kebab:kebab` is LOCAL-ONLY. The image starts from scratch each time, no production data ever touches it. Phase 1 acceptance: text labels the image as local-only. |
| T-1-21 | Denial of Service | non-idempotent migration locks build for parallel deploys | mitigate | Idempotency proven via `scripts/probe-migration-idempotent.sh` (Task 05-1 Step D). drizzle-kit's `__drizzle_migrations` journal prevents double-application. |
| T-1-22 | Elevation of Privilege | Neon role used by build pipeline has CREATE EXTENSION permission only on `vector` (allow-listed) | accept | Neon's allow-list mechanism limits which extensions a non-superuser role can create; pgvector is on the list. Future v0.4 vector-column migrations also use only allow-listed extensions. RESEARCH §14 footnote. |
| T-1-23 | Spoofing | Vercel preview deploys for forked PRs may have access to env vars | accept-and-defer | GitHub repos default to "no env vars on forked PR builds" — the Neon-Vercel integration creates isolated branches with limited credentials. Full anti-fork-abuse policy is v1.0 hardening per CONTEXT.md deferred items. |
| T-1-24 | Tampering | a contributor accidentally runs `bun db:push` against production | mitigate | CONTRIBUTING.md explicitly labels `db:push` as "LOCAL DEV ONLY — NEVER use against production." `db:migrate` is the only production-safe path per CONTEXT D-13. The build script uses `db:migrate` exclusively. |
</threat_model>

<verification>
After ALL four tasks complete:

- `bun run build` exits 0 locally (full pipeline: migrate idempotent + next build succeeds)
- `bun test` exits 0 — every Phase 1 test green (env, env.example, qstash, users.schema, topics.schema, votes.unique)
- `bun run scripts/probe-pgvector.ts` exits 0
- `bash scripts/probe-migration-idempotent.sh` exits 0
- `bun check:all` exits 0
- `package.json` `build` is exactly `"bun db:migrate && next build"` (single grep gate)
- `tests/db/setup.ts` TRUNCATE fixture is unconditional (PHASE_1_DB_READY gate removed)
- `CONTRIBUTING.md` documents Database Setup (Neon canonical + Docker fallback + db:* scripts + schema constraints)
- `docker-compose.yml` ships pgvector/pgvector:pg17 fallback
- Live Neon dev branch contains: `vector` extension; `topic_status` enum; `users` (with text id, FK to neon_auth.users_sync); `topics` (uuid id, gen_random_uuid default); `votes` (composite unique constraint)
- Vercel preview deploy succeeds (user-checkpoint approved) OR user explicitly skipped this gate (documented in SUMMARY)
</verification>

<success_criteria>
- INFRA-01 verified: `users.trust_score int default 0` in live DB; FK to neon_auth.users_sync(id)
- INFRA-02 verified: `topics` table with all columns + topic_status enum
- INFRA-03 verified: `votes` composite unique constraint enforced (or DDL-introspected)
- INFRA-07 verified: pgvector extension in `pg_extension` table
- INFRA-08 verified: QStash client constructs without throw; Vercel deploy includes the QSTASH_* env scaffold
- INFRA-09 verified: migrations idempotent; `.env.example` matches env.ts schema 1:1; no hardcoded secrets
- All 6 Phase 1 requirements close
- Wave 0 spec fulfilled: every test that was RED is now GREEN
- Phase 1 ROADMAP success criteria 1-5 ALL satisfied
- Plan 06 (Phase 2 — Magic-Link Auth) can begin with confidence
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation/01-05-SUMMARY.md` documenting:
- Migration apply log excerpt (showing 0000 + 0001 applied)
- Idempotency probe output (PASS line)
- pgvector probe output (PASS line)
- `bun run build` exit 0 confirmation (Vercel-equivalent)
- Vercel preview deploy URL OR explicit "user skipped per checkpoint" note
- All 6 INFRA-XX requirement IDs marked verified with their corresponding test/probe
- Files created in this plan (CONTRIBUTING.md additions, docker-compose.yml, tests/db/setup.ts edit, package.json build line change)
- One forward-looking note: "Phase 2 (Magic-Link Auth) starts here. The schema FK to neon_auth.users_sync is already in place — Phase 2 only needs to wire @neondatabase/auth, the route handler, and the +1 trust_score increment on first verified login."
</output>
